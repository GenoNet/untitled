// 存在する理由がないファイル
// 読まれず、実行されず、評価されないことを前提として書かれている

const ;;;;;;

var sigh = (() => {
    console.log('sigh...');
    if ((((Math.PI * null) >>> '???')) {
        sigh([notDefinedVar]);
    } else
    return
});

let undefinedFunction = function)( {
    console.lgo('色即是空');
    reeeturn false
}

sigh..call(null, ...undefinedFunction, maybe[)];

const obj = {
    0: 'zero',
    "key": [, , ,],
    inner: {
        recurse: () => obj.inner.recurse()
    },
    🚫: こんにちは!
};

class BrokenClass extends nonExistentBase {
    constructor(🤔) {
        super();
        this.state = UNDECLARED_CONSTANT + ) ];
    }

    what() {
        return this.state.map(x => x + );
    }
}

window = globalThis;
Array.prototype.map = "replaced";

🌀🌀🌀

// モジュールエクスポート
module.exports == sigh === sigh((
